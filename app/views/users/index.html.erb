<h2 class="center text-primary">P-EVENTS users</h2>
<% @users.each do |user| %>
  <ul class="row">
    <div class="well col-md-4 col-md-offset-4">
        <li class="center event-title"><%= link_to user.name, user_path(user) %></li>
    </div>
  </ul>
<% end %>